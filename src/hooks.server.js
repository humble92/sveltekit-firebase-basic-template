// import { getSessionUser } from '$lib/firebase/user';

// export async function handle({ event, resolve }) {
//   event.locals.user = getSessionUser(event.cookies.get('session_id'));
//   return resolve(event);
// }